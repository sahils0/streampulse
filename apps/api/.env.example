DB_HOST = localhost
DB_PORT = 5432
DB_NAME = streampulse
DB_USER = admin
DB_PASSWORD = yoursecret

JWT_PRIVATE_KEY=
JWT_PUBLIC_KEY=
JWT_EXPIRES_IN=15m

# --- Generate private key
# openssl genrsa -out private.pem 2048

# ---Derive public key
# openssl rsa -in private.pem -pubout -out public.pem

# --- Base64-encode both for .env
# base64 -w 0 private.pem   # → JWT_PRIVATE_KEY
# base64 -w 0 public.pem    # → JWT_PUBLIC_KEY


# openssl genrsa -out private.pem 2048
# openssl rsa -in private.pem -pubout -out public.pem

# node -e "
# const fs = require('fs');
# const priv = fs.readFileSync('private.pem', 'utf8');
# const pub  = fs.readFileSync('public.pem',  'utf8');
# console.log('JWT_PRIVATE_KEY=' + Buffer.from(priv).toString('base64'));
# console.log('JWT_PUBLIC_KEY='  + Buffer.from(pub).toString('base64'));
# "
